pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile'
            dir 'Jenkins/Slave'
            label 'jenkins-k8spipeline-slave'
        }
    }
    stages {
        stage('Setup Credentials') {
            
        }
    }
}